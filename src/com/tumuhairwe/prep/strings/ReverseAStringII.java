package com.tumuhairwe.prep.strings;

/**
 * LeetCode 541 (reverse a string)
 * Given a string s and an integer k, reverse the 1st k characters for every 2k characters
 * counting from the start of the string
 * If there are fewer than k characters left, reverse all of them.
 * If there are less an 2k but >= k chars, reverse the 1st k chars and leave the others unchanged
 *
 * ref: https://leetcode.com/problems/reverse-string-ii/
 */
public class ReverseAStringII {
    //public
}
